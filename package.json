{
  "name": "ai-team-game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "client-install": "cd client && npm install",
    "client-build": "cd client && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}
